# Shared agent tiers to Codex models.
# Keep this file in sync with the model field in .claude/agents/*.md and
# .claude/skills/*/SKILL.md. Codex must pass the resolved model explicitly
# when spawning a studio agent.

[tiers]
haiku = "gpt-5.6-luna"
sonnet = "gpt-5.6-terra"
opus = "gpt-5.6-sol"

# Canonical source: ../SKILL.md
interface:
  display_name: "Scope Check"
  short_description: "Analyze a feature or sprint for scope creep by comparing current scope against the original plan. Flags additions, quantifies bloat, and recommends cuts. Use..."
  default_prompt: "Use the $scope-check skill for the requested task."
policy:
  allow_implicit_invocation: true

# Canonical source: ../SKILL.md
interface:
  display_name: "Balance Check"
  short_description: "Analyzes game balance data files, formulas, and configuration to identify outliers, broken progressions, degenerate strategies, and economy imbalances. Use a..."
  default_prompt: "Use the $balance-check skill for the requested task."
policy:
  allow_implicit_invocation: true

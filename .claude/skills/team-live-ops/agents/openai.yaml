# Canonical source: ../SKILL.md
interface:
  display_name: "Team Live Ops"
  short_description: "Orchestrate the live-ops team for post-launch content planning: coordinates live-ops-designer, economy-designer, analytics-engineer, community-manager, write..."
  default_prompt: "Use the $team-live-ops skill for the requested task."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Reverse Document"
  short_description: "Generate design or architecture documents from existing implementation. Works backwards from code/prototypes to create missing planning docs."
  default_prompt: "Use the $reverse-document skill for the requested task."
policy:
  allow_implicit_invocation: true

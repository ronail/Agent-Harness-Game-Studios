interface:
  display_name: "Setup Engine"
  short_description: "Configure the project's game engine and version. Pins the engine in CLAUDE.md, detects knowledge gaps, and populates engine reference docs via WebSearch when..."
  default_prompt: "Use the $setup-engine skill for the requested task."
policy:
  allow_implicit_invocation: true

# Canonical source: ../SKILL.md
interface:
  display_name: "Release Checklist"
  short_description: "Generates a comprehensive pre-release validation checklist covering build verification, certification requirements, store metadata, and launch readiness."
  default_prompt: "Use the $release-checklist skill for the requested task."
policy:
  allow_implicit_invocation: true

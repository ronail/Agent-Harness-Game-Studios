# Canonical source: ../SKILL.md
interface:
  display_name: "Day One Patch"
  short_description: "Prepare a day-one patch for a game launch. Scopes, prioritises, implements, and QA-gates a focused patch addressing known issues discovered after gold master..."
  default_prompt: "Use the $day-one-patch skill for the requested task."
policy:
  allow_implicit_invocation: true

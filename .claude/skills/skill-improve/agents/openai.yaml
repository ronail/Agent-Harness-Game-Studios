# Canonical source: ../SKILL.md
interface:
  display_name: "Skill Improve"
  short_description: "Improve a skill using a test-fix-retest loop. Runs static checks, proposes targeted fixes, rewrites the skill, re-tests, and keeps or reverts based on score..."
  default_prompt: "Use the $skill-improve skill for the requested task."
policy:
  allow_implicit_invocation: true

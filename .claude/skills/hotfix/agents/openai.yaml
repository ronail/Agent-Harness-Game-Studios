interface:
  display_name: "Hotfix"
  short_description: "Emergency fix workflow that bypasses normal sprint processes with a full audit trail. Creates hotfix branch, tracks approvals, and ensures the fix is backpor..."
  default_prompt: "Use the $hotfix skill for the requested task."
policy:
  allow_implicit_invocation: true

# Canonical source: ../SKILL.md
interface:
  display_name: "Soak Test"
  short_description: "Generate a soak test protocol for extended play sessions. Defines what to observe, measure, and log during long play sessions to surface slow leaks, fatigue..."
  default_prompt: "Use the $soak-test skill for the requested task."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Bug Report"
  short_description: "Creates a structured bug report from a description, or analyzes code to identify potential bugs. Ensures every bug report has full reproduction steps, severi..."
  default_prompt: "Use the $bug-report skill for the requested task."
policy:
  allow_implicit_invocation: true

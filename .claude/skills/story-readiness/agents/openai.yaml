# Canonical source: ../SKILL.md
interface:
  display_name: "Story Readiness"
  short_description: "Validate that a story file is implementation-ready. Checks for embedded GDD requirements, ADR references, engine notes, clear acceptance criteria, and no ope..."
  default_prompt: "Use the $story-readiness skill for the requested task."
policy:
  allow_implicit_invocation: true

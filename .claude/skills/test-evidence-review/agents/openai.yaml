interface:
  display_name: "Test Evidence Review"
  short_description: "Quality review of test files and manual evidence documents. Goes beyond existence checks — evaluates assertion coverage, edge case handling, naming conventio..."
  default_prompt: "Use the $test-evidence-review skill for the requested task."
policy:
  allow_implicit_invocation: true

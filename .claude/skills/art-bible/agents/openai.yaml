interface:
  display_name: "Art Bible"
  short_description: "Guided, section-by-section Art Bible authoring. Creates the visual identity specification that gates all asset production. Run after /brainstorm is approved..."
  default_prompt: "Use the $art-bible skill for the requested task."
policy:
  allow_implicit_invocation: true

# Canonical source: ../SKILL.md
interface:
  display_name: "Estimate"
  short_description: "Estimates task effort by analyzing complexity, dependencies, historical velocity, and risk factors. Produces a structured estimate with confidence levels."
  default_prompt: "Use the $estimate skill for the requested task."
policy:
  allow_implicit_invocation: true

# Canonical source: ../SKILL.md
interface:
  display_name: "Architecture Review"
  short_description: "Validates completeness and consistency of the project architecture against all GDDs. Builds a traceability matrix mapping every GDD technical requirement to..."
  default_prompt: "Use the $architecture-review skill for the requested task."
policy:
  allow_implicit_invocation: true

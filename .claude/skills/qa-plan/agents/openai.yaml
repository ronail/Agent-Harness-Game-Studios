# Canonical source: ../SKILL.md
interface:
  display_name: "QA Plan"
  short_description: "Generate a QA test plan for a sprint or feature. Reads GDDs and story files, classifies stories by test type (Logic/Integration/Visual/UI), and produces a st..."
  default_prompt: "Use the $qa-plan skill for the requested task."
policy:
  allow_implicit_invocation: true

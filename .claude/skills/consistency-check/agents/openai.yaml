# Canonical source: ../SKILL.md
interface:
  display_name: "Consistency Check"
  short_description: "Scan all GDDs against the entity registry to detect cross-document inconsistencies: same entity with different stats, same item with different values, same f..."
  default_prompt: "Use the $consistency-check skill for the requested task."
policy:
  allow_implicit_invocation: true

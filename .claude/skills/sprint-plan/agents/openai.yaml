interface:
  display_name: "Sprint Plan"
  short_description: "Generates a new sprint plan or updates an existing one based on the current milestone, completed work, and available capacity. Pulls context from production..."
  default_prompt: "Use the $sprint-plan skill for the requested task."
policy:
  allow_implicit_invocation: true

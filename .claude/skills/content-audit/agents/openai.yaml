interface:
  display_name: "Content Audit"
  short_description: "Audit GDD-specified content counts against implemented content. Identifies what's planned vs built."
  default_prompt: "Use the $content-audit skill for the requested task."
policy:
  allow_implicit_invocation: true

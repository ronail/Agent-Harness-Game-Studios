interface:
  display_name: "Brainstorm"
  short_description: "Guided game concept ideation — from zero idea to a structured game concept document. Uses professional studio ideation techniques, player psychology framewor..."
  default_prompt: "Use the $brainstorm skill for the requested task."
policy:
  allow_implicit_invocation: true

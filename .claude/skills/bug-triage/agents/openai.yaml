# Canonical source: ../SKILL.md
interface:
  display_name: "Bug Triage"
  short_description: "Read all open bugs in production/qa/bugs/, re-evaluate priority vs. severity, assign to sprints, surface systemic trends, and produce a triage report. Run at..."
  default_prompt: "Use the $bug-triage skill for the requested task."
policy:
  allow_implicit_invocation: true

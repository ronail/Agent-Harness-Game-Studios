# Canonical source: ../SKILL.md
interface:
  display_name: "Story Done"
  short_description: "End-of-story completion review. Reads the story file, verifies each acceptance criterion against the implementation, checks for GDD/ADR deviations, prompts c..."
  default_prompt: "Use the $story-done skill for the requested task."
policy:
  allow_implicit_invocation: true

# Canonical source: ../SKILL.md
interface:
  display_name: "Asset Audit"
  short_description: "Audits game assets for compliance with naming conventions, file size budgets, format standards, and pipeline requirements. Identifies orphaned assets, missin..."
  default_prompt: "Use the $asset-audit skill for the requested task."
policy:
  allow_implicit_invocation: true

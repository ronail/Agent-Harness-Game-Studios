# Canonical source: ../SKILL.md
interface:
  display_name: "Create Stories"
  short_description: "Break a single epic into implementable story files. Reads the epic, its GDD, governing ADRs, and control manifest. Each story embeds its GDD requirement TR-I..."
  default_prompt: "Use the $create-stories skill for the requested task."
policy:
  allow_implicit_invocation: true

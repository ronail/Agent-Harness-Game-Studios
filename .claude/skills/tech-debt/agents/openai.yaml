# Canonical source: ../SKILL.md
interface:
  display_name: "Tech Debt"
  short_description: "Track, categorize, and prioritize technical debt across the codebase. Scans for debt indicators, maintains a debt register, and recommends repayment scheduling."
  default_prompt: "Use the $tech-debt skill for the requested task."
policy:
  allow_implicit_invocation: true

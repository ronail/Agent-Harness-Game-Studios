interface:
  display_name: "Gate Check"
  short_description: "Validate readiness to advance between development phases. Produces a PASS/CONCERNS/FAIL verdict with specific blockers and required artifacts. Use when user..."
  default_prompt: "Use the $gate-check skill for the requested task."
policy:
  allow_implicit_invocation: true

# Canonical source: ../SKILL.md
interface:
  display_name: "Design Review"
  short_description: "Reviews a game design document for completeness, internal consistency, implementability, and adherence to project design standards. Run this before handing a..."
  default_prompt: "Use the $design-review skill for the requested task."
policy:
  allow_implicit_invocation: true

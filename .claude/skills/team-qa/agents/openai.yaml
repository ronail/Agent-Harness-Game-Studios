# Canonical source: ../SKILL.md
interface:
  display_name: "Team QA"
  short_description: "Orchestrate the QA team through a full testing cycle. Coordinates qa-lead (strategy + test plan) and qa-tester (test case writing + bug reporting) to produce..."
  default_prompt: "Use the $team-qa skill for the requested task."
policy:
  allow_implicit_invocation: true

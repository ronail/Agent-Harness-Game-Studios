interface:
  display_name: "Changelog"
  short_description: "Auto-generates a changelog from git commits, sprint data, and design documents. Produces both internal and player-facing versions."
  default_prompt: "Use the $changelog skill for the requested task."
policy:
  allow_implicit_invocation: true

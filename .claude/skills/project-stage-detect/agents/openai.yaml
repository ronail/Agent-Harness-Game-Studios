interface:
  display_name: "Project Stage Detect"
  short_description: "Automatically analyze project state, detect stage, identify gaps, and recommend next steps based on existing artifacts. Use when user asks 'where are we in d..."
  default_prompt: "Use the $project-stage-detect skill for the requested task."
policy:
  allow_implicit_invocation: true

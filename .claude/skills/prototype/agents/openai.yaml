interface:
  display_name: "Prototype"
  short_description: "Concept prototype — validate the core idea is worth designing before writing GDDs. Run right after /brainstorm and /setup-engine. Routes to HTML, Engine, or..."
  default_prompt: "Use the $prototype skill for the requested task."
policy:
  allow_implicit_invocation: true

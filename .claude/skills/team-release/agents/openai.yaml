# Canonical source: ../SKILL.md
interface:
  display_name: "Team Release"
  short_description: "Orchestrate the release team: coordinates release-manager, qa-lead, devops-engineer, and producer to execute a release from candidate to deployment."
  default_prompt: "Use the $team-release skill for the requested task."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "UX Design"
  short_description: "Guided, section-by-section UX spec authoring for a screen, flow, or HUD. Reads game concept, player journey, and relevant GDDs to provide context-aware desig..."
  default_prompt: "Use the $ux-design skill for the requested task."
policy:
  allow_implicit_invocation: true

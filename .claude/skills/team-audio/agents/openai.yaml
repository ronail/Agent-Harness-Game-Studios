# Canonical source: ../SKILL.md
interface:
  display_name: "Team Audio"
  short_description: "Orchestrate audio team: audio-director + sound-designer + technical-artist + gameplay-programmer for full audio pipeline from direction to implementation."
  default_prompt: "Use the $team-audio skill for the requested task."
policy:
  allow_implicit_invocation: true

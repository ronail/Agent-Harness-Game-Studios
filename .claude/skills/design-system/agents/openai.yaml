# Canonical source: ../SKILL.md
interface:
  display_name: "Design System"
  short_description: "Guided, section-by-section GDD authoring for a single game system. Gathers context from existing docs, walks through each required section collaboratively, c..."
  default_prompt: "Use the $design-system skill for the requested task."
policy:
  allow_implicit_invocation: true

# Canonical source: ../SKILL.md
interface:
  display_name: "Start"
  short_description: "First-time onboarding — asks where you are, then guides you to the right workflow. No assumptions."
  default_prompt: "Use the $start skill for the requested task."
policy:
  allow_implicit_invocation: true

# Canonical source: ../SKILL.md
interface:
  display_name: "Localize"
  short_description: "Full localization pipeline: scan for hardcoded strings, extract and manage string tables, validate translations, generate translator briefings, run cultural/..."
  default_prompt: "Use the $localize skill for the requested task."
policy:
  allow_implicit_invocation: true

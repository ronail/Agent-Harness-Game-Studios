interface:
  display_name: "Create Epics"
  short_description: "Translate approved GDDs + architecture into epics — one epic per architectural module. Defines scope, governing ADRs, engine risk, and untraced requirements...."
  default_prompt: "Use the $create-epics skill for the requested task."
policy:
  allow_implicit_invocation: true
